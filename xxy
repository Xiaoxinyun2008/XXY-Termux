# 1. 深度清理旧版
rm -f $PREFIX/bin/xxy
rm -rf $PREFIX/tmp/xxy_*
rm -rf $PREFIX/tmp/.X11-unix
pkill -9 termux-x11 2>/dev/null
pkill -9 pulseaudio 2>/dev/null

# 2. 写入脚本
cat > $PREFIX/bin/xxy << 'INSTALL_EOF'
#!/data/data/com.termux/files/usr/bin/bash
# ==============================================================================
# PROJECT: XXY (Final Edition v38.0)
# FIX: 文件夹级容器检测、标准启动命令、详细结束报告
# ==============================================================================

# --- 0. 全局配置 ---
export VERSION="v38.0"
# 稳定加速源
export PROXY_URL="https://ghproxy.net/"
export UPDATE_URL="${PROXY_URL}https://raw.githubusercontent.com/Xiaoxinyun2008/XXY-Termux/main/xxy"
export GITHUB_URL="https://github.com/Xiaoxinyun2008/XXY-Termux"
export PREFIX="/data/data/com.termux/files/usr"
export TMPDIR="$PREFIX/tmp"
# [核心] 容器物理存储路径
export ROOTFS_BASE="$PREFIX/var/lib/proot-distro/installed-rootfs"
export PD_CONF="$PREFIX/etc/proot-distro"
export TERMUX_PKG_OPTS="-y -o Dpkg::Options::=--force-confnew" 

[ ! -d "$TMPDIR" ] && mkdir -p "$TMPDIR"

R=$(tput setaf 1); G=$(tput setaf 2); Y=$(tput setaf 3)
B=$(tput setaf 4); P=$(tput setaf 5); C=$(tput setaf 6)
W=$(tput setaf 7); NC=$(tput sgr0)

cleanup() {
    tput cnorm; stty echo; rm -f "$TMPDIR"/xxy_* 2>/dev/null
}
trap cleanup EXIT INT TERM

# --- 1. 基础环境 ---

get_ip() {
    local ip=""
    if command -v ip >/dev/null; then ip=$(ip route get 1.1.1.1 2>/dev/null | awk '{print $7}'); fi
    if [ -z "$ip" ] && command -v ifconfig >/dev/null; then
        ip=$(ifconfig 2>/dev/null | grep -oE 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1' | awk '{print $2}' | sed 's/addr://' | head -n 1)
    fi
    echo "${ip:-127.0.0.1}"
}

check_dependencies() {
    if [ -f "$PREFIX/etc/xxy_checked_v38" ]; then return; fi
    clear; echo -e "${C}=== 环境自检 ===${NC}"
    
    DEPS="proot-distro pulseaudio wget curl ncurses-utils grep sed awk fastfetch net-tools"
    MISSING=""
    for dep in $DEPS; do 
        if ! command -v $dep &>/dev/null; then MISSING="$MISSING $dep"; fi
    done
    
    if ! command -v termux-x11 &>/dev/null; then 
        pkg install x11-repo $TERMUX_PKG_OPTS >/dev/null 2>&1
        MISSING="$MISSING termux-x11-nightly"
    fi
    
    if [ -n "$MISSING" ]; then
        echo -e "${Y}安装缺失库: $MISSING ...${NC}"
        pkg update -y -k >/dev/null 2>&1
        pkg install $MISSING $TERMUX_PKG_OPTS
    fi
    touch "$PREFIX/etc/xxy_checked_v38"
}

optimize_network() {
    local TARGETS="ubuntu debian archlinux alpine"
    for distro in $TARGETS; do
        local conf_file="$PD_CONF/$distro.sh"
        if [ -f "$conf_file" ]; then
            sed -i 's|https://.*/https://github.com|https://github.com|g' "$conf_file"
            sed -i "s|https://github.com|${PROXY_URL}https://github.com|g" "$conf_file"
        fi
    done
}

# --- 2. 核心逻辑 (用户指定检测方案) ---

# [核心] 文件夹检测法
get_systems() {
    # 只要 installed-rootfs 下有文件夹，就视为已安装
    if [ -d "$ROOTFS_BASE" ]; then
        # 列出目录名，即为容器 Alias
        ls -1 "$ROOTFS_BASE" 2>/dev/null
    fi
}

WORKER_SCRIPT="$TMPDIR/xxy_worker_$$.sh"
INJECT_SCRIPT="$TMPDIR/xxy_inject_$$.sh"

create_worker_script() {
    rm -f "$WORKER_SCRIPT"
    cat << 'EOF' > "$WORKER_SCRIPT"
#!/bin/sh
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
action=$1
if command -v apt >/dev/null; then
    INSTALL="apt install -y"; UPDATE="apt update -y"; export DEBIAN_FRONTEND=noninteractive; is_debian=true
elif command -v pacman >/dev/null; then
    INSTALL="pacman -S --noconfirm"; UPDATE="pacman -Sy --noconfirm"; is_arch=true
elif command -v apk >/dev/null; then
    INSTALL="apk add"; UPDATE="apk update"; is_alpine=true
else echo "Unknown PM"; exit 1; fi

case "$action" in
  change_source)
    if [ "$is_debian" = true ]; then
        echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy main restricted universe multiverse" > /etc/apt/sources.list
        echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-updates main restricted universe multiverse" >> /etc/apt/sources.list
        echo "deb https://mirrors.tuna.tsinghua.edu.cn/ubuntu/ jammy-security main restricted universe multiverse" >> /etc/apt/sources.list
    elif [ "$is_alpine" = true ]; then
        sed -i 's/dl-cdn.alpinelinux.org/mirrors.ustc.edu.cn/g' /etc/apk/repositories
    fi
    $UPDATE ;;
  install_gui)
    if [ "$is_debian" = true ]; then $INSTALL xfce4 xfce4-goodies dbus-x11 tigervnc-standalone-server
    elif [ "$is_arch" = true ]; then $INSTALL xfce4 dbus tigervnc
    elif [ "$is_alpine" = true ]; then $INSTALL xfce4 dbus-x11 tigervnc; fi
    mkdir -p ~/.vnc; echo "123456" | vncpasswd -f > ~/.vnc/passwd; chmod 600 ~/.vnc/passwd ;;
  install_fastfetch)
    $INSTALL fastfetch || $INSTALL neofetch
    if command -v fastfetch >/dev/null; then echo "fastfetch" > /tmp/fetch_bin
    else echo "none" > /tmp/fetch_bin; fi ;;
  install_cn)
    if [ "$is_debian" = true ]; then $INSTALL locales fonts-noto-cjk; echo "zh_CN.UTF-8 UTF-8" > /etc/locale.gen && locale-gen; echo "export LANG=zh_CN.UTF-8" > /etc/profile.d/xxy_lang.sh; fi ;;
esac
EOF
    chmod 777 "$WORKER_SCRIPT"
}

run_task() {
    local sys=$1; local cmd=$2; local desc=$3
    local inner_script="/tmp/$(basename "$WORKER_SCRIPT")"
    tput cup 23 0; echo -ne "${Y}⚙️  [$sys] $desc...${NC}"
    # 使用 shared-tmp 映射脚本
    if proot-distro login "$sys" --shared-tmp -- /bin/sh "$inner_script" "$cmd" >/dev/null 2>&1; then
        tput cup 23 0; echo -ne "${G}✅ [$sys] $desc 成功          ${NC}"
    else
        tput cup 23 0; echo -ne "${R}❌ [$sys] $desc 失败          ${NC}"
    fi
}

select_menu() {
    mapfile -t SYS_ARRAY < <(get_systems)
    if [ ${#SYS_ARRAY[@]} -eq 0 ]; then
        echo -e "\n${R}⚠️  未检测到系统 (installed-rootfs 为空)${NC}"
        echo "请先选择 [2] 安装系统"
        read -n 1 -s -r -p "..."
        return 1
    fi
    for i in "${!SYS_ARRAY[@]}"; do echo -e "${G}$((i+1)).${NC} ${SYS_ARRAY[$i]}"; done
    echo -e "${W}0.${NC} 返回"
    tput cnorm; stty echo; read -p "👉 序号: " choice; stty -echo; tput civis
    if [ "$choice" == "0" ] || [ -z "$choice" ]; then return 1; fi
    if [[ "$choice" =~ ^[0-9]+$ ]] && [ "$choice" -ge 1 ] && [ "$choice" -le "${#SYS_ARRAY[@]}" ]; then
        echo "${SYS_ARRAY[$((choice-1))]}"
        return 0
    fi
    return 1
}

# --- 功能模块 ---

do_login() {
    clear; echo -e "${C}=== 登录系统 ===${NC}"
    TARGET=$(select_menu) || return
    create_worker_script
    local inner_script="/tmp/$(basename "$WORKER_SCRIPT")"
    local inner_inject="/tmp/$(basename "$INJECT_SCRIPT")"
    
    echo -e "${B}启动容器...${NC}"
    # 预注入美化 (不阻塞)
    proot-distro login "$TARGET" --shared-tmp -- /bin/sh "$inner_script" "install_fastfetch" >/dev/null 2>&1
    
    # 注入 bashrc
    cat > "$INJECT_SCRIPT" << 'EOF'
RC=~/.bashrc
[ -f /etc/profile.d/xxy_lang.sh ] && . /etc/profile.d/xxy_lang.sh
if [ -f /tmp/fetch_bin ]; then
    TOOL=$(cat /tmp/fetch_bin); rm -f /tmp/fetch_bin
    if [ "$TOOL" != "none" ] && ! grep -q "$TOOL" "$RC" 2>/dev/null; then
        echo "" >> "$RC"; echo "clear" >> "$RC"; echo "$TOOL" >> "$RC"
    fi
fi
EOF
    chmod 777 "$INJECT_SCRIPT"
    proot-distro login "$TARGET" --shared-tmp -- /bin/sh "$inner_inject" >/dev/null 2>&1
    
    cleanup
    # [核心] 使用对应的 Termux 启动命令 (proot-distro login)
    proot-distro login "$TARGET"
    
    tput civis; stty -echo; draw_ui
}

do_install() {
    clear; echo -e "${C}=== 安装系统 (加速版) ===${NC}"
    DISTROS=("ubuntu" "debian" "archlinux" "alpine")
    for i in "${!DISTROS[@]}"; do echo -e "${W}$((i+1)).${NC} ${DISTROS[$i]}"; done
    echo -e "${W}0.${NC} 返回"
    tput cnorm; stty echo; read -p "👉 选择: " idx; stty -echo; tput civis
    if [ "$idx" == "0" ]; then return; fi
    if [[ "$idx" =~ ^[0-9]+$ ]] && [ "$idx" -ge 1 ] && [ "$idx" -le "${#DISTROS[@]}" ]; then
        TARGET=${DISTROS[$((idx-1))]}
        
        # 检测文件夹是否存在
        if [ -d "$ROOTFS_BASE/$TARGET" ]; then
            # 按照要求，文件夹存在即视为已安装
            echo -e "\n${G}系统目录已存在 ($TARGET)，无需重复安装。${NC}"
            read -n 1 -s -r -p "..."; return
        fi
        
        optimize_network
        echo -e "\n${Y}下载中...${NC}"; tput cnorm
        if proot-distro install $TARGET; then
            tput civis; create_worker_script
            run_task "$TARGET" "change_source" "换国内源"
            run_task "$TARGET" "install_cn" "中文环境"
            run_task "$TARGET" "install_fastfetch" "美化"
            echo -e "\n${G}🎉 安装完成！${NC}"; read -n 1 -s -r -p "..."
        else
            echo -e "\n${R}❌ 失败，清理残余...${NC}"
            proot-distro remove $TARGET >/dev/null 2>&1
            read -n 1 -s -r -p "..."
        fi
    fi
}

do_gui() {
    clear; echo -e "${C}=== 启动桌面 ===${NC}"
    TARGET=$(select_menu) || return
    create_worker_script
    
    echo -ne "🔍 检查桌面...\r"
    if ! proot-distro login "$TARGET" -- bash -c "command -v startxfce4" >/dev/null 2>&1; then
        echo -e "\n${Y}正在安装桌面环境...${NC}"
        run_task "$TARGET" "install_gui" "部署 GUI"
    fi
    
    echo -e "\n${G}模式:${NC}\n1. Termux:X11\n2. VNC Viewer\n0. 返回"
    tput cnorm; stty echo; read -p "选择: " m; stty -echo; tput civis
    
    if [ "$m" == "1" ]; then
        pulseaudio --kill >/dev/null 2>&1; pkill -f com.termux.x11 >/dev/null 2>&1; rm -rf "$TMPDIR/.X11-unix"
        pulseaudio --start --load="module-native-protocol-tcp auth-ip-acl=127.0.0.1 auth-anonymous=1" --exit-idle-time=-1
        am start --user 0 -n com.termux.x11/com.termux.x11.MainActivity >/dev/null 2>&1; sleep 1
        termux-x11 :0 >/dev/null 2>&1 &
        CMD="export DISPLAY=:0 PULSE_SERVER=127.0.0.1; dbus-launch --exit-with-session startxfce4"
        proot-distro login "$TARGET" --shared-tmp -- bash -c "$CMD"
    elif [ "$m" == "2" ]; then
        CMD="vncserver -kill :1 >/dev/null 2>&1; rm -rf /tmp/.X1-lock /tmp/.X11-unix/X1; vncserver :1 -geometry 1280x720"
        proot-distro login "$TARGET" -- bash -c "$CMD"
        echo -e "\n${G}VNC ON${NC}: 127.0.0.1:5901 | Pass: 123456"; read -n 1 -s -r -p "..."
    fi
}

do_github() {
    clear; echo -e "${C}=== 访问 GitHub ===${NC}"
    if ! command -v lynx &>/dev/null; then
        echo -e "${Y}Install Lynx...${NC}"; tput cnorm; pkg install lynx $TERMUX_PKG_OPTS; tput civis
    fi
    lynx "$GITHUB_URL"; tput civis; stty -echo; draw_ui
}

do_update() {
    clear; echo -e "${C}=== 检查更新 ===${NC}"
    local temp="$TMPDIR/xxy_tmp"; rm -f "$temp"
    local http_code=$(curl -L -s -m 5 -o "$temp" -w "%{http_code}" "$UPDATE_URL")
    if [ "$http_code" != "200" ]; then
        echo -e "\n${R}❌ 网络错误 ($http_code)${NC}"; read -n 1 -s -r -p "..."; return
    fi
    if ! grep -q "PROJECT: XXY" "$temp"; then echo -e "\n${R}❌ 校验失败${NC}"; read -n 1 -s -r -p "..."; return; fi
    local remote_ver=$(grep "export VERSION=" "$temp" | cut -d'"' -f2)
    echo -e "\n本地: $VERSION | 云端: $remote_ver"
    tput cnorm; stty echo; echo ""; read -p "更新? (y/n): " c; stty -echo; tput civis
    if [[ "$c" == "y" ]]; then mv "$temp" "$PREFIX/bin/xxy"; chmod 755 "$PREFIX/bin/xxy"; echo -e "${G}OK${NC}"; exit 0; fi
}

# --- Main UI ---
update_status() {
    if (( FRAME % 50 == 0 )); then MEM=$(free -m 2>/dev/null | awk 'NR==2{printf "%.1fG", $3/1024}' || echo "N/A"); fi
    tput cup 0 34; printf "${B}╭── STATUS ──────────────────────────╮${NC}"
    tput cup 1 34; printf "${B}│${NC} TIME: ${W}%-5s${NC} MEM: ${P}%-5s${NC}         ${B}│${NC}" "$(date "+%H:%M")" "$MEM"
    tput cup 2 34; printf "${B}│${NC} IP: ${GR}%-15s${NC}             ${B}│${NC}" "$(get_ip)"
    tput cup 3 34; printf "${B}╰────────────────────────────────────╯${NC}"
}

draw_ui() {
    clear
    cat << EOF
${C}
██╗  ██╗██╗  ██╗██╗   ██╗ ${W}XXY Termux${C}
╚██╗██╔╝╚██╗██╔╝╚██╗ ██╔╝ ${GR}${VERSION}${C}
 ╚███╔╝  ╚███╔╝  ╚████╔╝  ${W}Final${C}
 ██╔██╗  ██╔██╗   ╚██╔╝   ${NC}
${GR}──────────────────────────────────────────${NC}
${GR}│${NC} ${W}1.${NC} 登录系统 (Login)    ${GR}│${NC} ${W}3.${NC} 启动桌面 (GUI)
${GR}│${NC} ${W}2.${NC} 安装系统 (Install)  ${GR}│${NC} ${W}5.${NC} 检查更新 (Update)
${GR}│${NC} ${W}6.${NC} 访问 GitHub (Lynx)  ${GR}│${NC} ${W}0.${NC} 退出程序 (Exit)
${GR}──────────────────────────────────────────${NC}
EOF
    if command -v fastfetch >/dev/null; then fastfetch --config structure:OS:Kernel:Uptime --logo none 2>/dev/null | sed 's/^/  /'; fi
}

check_dependencies; create_worker_script; tput civis; stty -echo
draw_ui; INPUT_BUF=""; FRAME=0; MEM="Wait"
while true; do
    update_status; ((FRAME++))
    tput cup 22 0; echo -ne "${C}➤ 序号: ${W}${INPUT_BUF}_        ${NC}"
    read -t 0.05 -n 1 key
    if [[ $? -eq 0 ]]; then
        if [[ -z "$key" ]]; then
            if [[ -n "$INPUT_BUF" ]]; then
                case $INPUT_BUF in 1)do_login;; 2)do_install;; 3)do_gui;; 5)do_update;; 6)do_github;; 0)exit 0;; esac
                tput civis; stty -echo; draw_ui; INPUT_BUF=""
            fi
        elif [[ "$key" == $'\x7f' || "$key" == $'\b' ]]; then INPUT_BUF="${INPUT_BUF%?}"
        elif [[ "$key" == $'\e' ]]; then read -t 0.01 -n 2
        else INPUT_BUF="$INPUT_BUF$key"; fi
    fi
done
INSTALL_EOF

chmod 755 $PREFIX/bin/xxy
sed -i 's/\r$//' $PREFIX/bin/xxy

# --- 3. 详细结束报告 (用户要求) ---
clear
echo -e "\033[1;32m🎉 XXY v38.0 (完美版) 部署完成！\033[0m"
echo -e "\033[1;34m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"

echo -e "\033[1;36m📦 [核心依赖库清单]\033[0m"
echo -e "  • \033[37m系统核心:\033[0m proot-distro (容器管理), ncurses-utils (UI)"
echo -e "  • \033[37m图形支持:\033[0m x11-repo, termux-x11-nightly, pulseaudio"
echo -e "  • \033[37m网络工具:\033[0m wget, curl, net-tools (含IP检测)"
echo -e "  • \033[37m美化组件:\033[0m fastfetch (系统信息展示)"

echo -e "\n\033[1;33m🔧 [v38.0 版本改进]\033[0m"
echo -e "  1. \033[32m容器检测:\033[0m 采用物理目录 (installed-rootfs) 检测法，100% 识别率。"
echo -e "  2. \033[32m启动逻辑:\033[0m 严格调用 proot-distro login 标准命令，修复挂载错误。"
echo -e "  3. \033[32m网络优化:\033[0m 集成 ghproxy.net 稳定代理，解决下载 404 问题。"
echo -e "  4. \033[32m权限穿透:\033[0m 修复容器内 Permission Denied 问题。"

echo -e "\n\033[1;35m🚀 [XXY 命令功能]\033[0m"
echo -e "  • 输入 \033[1;37;42m 1 \033[0m : 登录系统 (自动清屏+显示系统信息)"
echo -e "  • 输入 \033[1;37;42m 2 \033[0m : 安装 Linux (自动配置中文+国内源)"
echo -e "  • 输入 \033[1;37;42m 3 \033[0m : 启动桌面 (Termux:X11 / VNC 局域网)"
echo -e "\033[1;34m━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\033[0m"

echo -e "\n\033[1;32m👉 请输入 \033[1;37;42m xxy \033[1;32m 启动！\033[0m"




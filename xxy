#!/data/data/com.termux/files/usr/bin/bash

# ==============================================================================
# 📱 XXY v4.0 终极生产力工具箱
# 整合：Shizuku自动修复 | Debian生产力环境 | VSCode | 二级菜单
# ==============================================================================

# --- [0] 全局配置 ---
export PREFIX="/data/data/com.termux/files/usr"
export HOME="/data/data/com.termux/files/home"
export DISTRO_NAME="debian"
export ROOTFS="$PREFIX/var/lib/proot-distro/installed-rootfs/$DISTRO_NAME"
export TMP_WORKER="$PREFIX/tmp/worker_script.sh"

# 颜色定义
R=$(tput setaf 1); G=$(tput setaf 2); Y=$(tput setaf 3)
B=$(tput setaf 4); C=$(tput setaf 6); W=$(tput setaf 7); NC=$(tput sgr0)

# 信号捕获
trap 'echo -e "\n${G}[XXY] 已安全退出。${NC}"; exit 0' EXIT INT TERM

# ==============================================================================
# 🛠️ 核心模块：容器内部构建脚本 (Worker)
# ==============================================================================
create_worker_script() {
    mkdir -p "$PREFIX/tmp"
    cat > "$TMP_WORKER" << 'EOF'
#!/bin/bash
set -e

# [1] 换源 (清华源 Debian Bookworm)
echo ">>> [内部] 正在配置国内源..."
cat > /etc/apt/sources.list << SOURCES
deb https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm main contrib non-free non-free-firmware
deb https://mirrors.tuna.tsinghua.edu.cn/debian/ bookworm-updates main contrib non-free non-free-firmware
deb https://mirrors.tuna.tsinghua.edu.cn/debian-security bookworm-security main contrib non-free non-free-firmware
SOURCES

# [2] 更新并安装基础软件
echo ">>> [内部] 正在安装桌面和生产力工具 (耗时较长)..."
apt update -y
export DEBIAN_FRONTEND=noninteractive

# 安装核心组件：桌面(XFCE)、浏览器(Firefox)、字体(Noto)、输入法(Fcitx5)、开发工具(Git/Python)
apt install -y sudo nano wget curl git build-essential python3-full \
    xfce4 xfce4-goodies xfce4-terminal dbus-x11 \
    firefox-esr \
    fonts-noto-cjk fonts-noto-color-emoji \
    fcitx5 fcitx5-chinese-addons fcitx5-frontend-gtk4 fcitx5-frontend-gtk3 fcitx5-frontend-qt5 \
    im-config

# [3] 中文环境配置
echo ">>> [内部] 生成中文 Locale..."
apt install -y locales
sed -i 's/# zh_CN.UTF-8 UTF-8/zh_CN.UTF-8 UTF-8/' /etc/locale.gen
locale-gen zh_CN.UTF-8
update-locale LANG=zh_CN.UTF-8 LANGUAGE=zh_CN:zh

# [4] 环境变量 (修复输入法和显示)
echo ">>> [内部] 配置环境变量..."
cat > /etc/profile.d/xxy_env.sh << 'ENV_EOF'
export LANG=zh_CN.UTF-8
export LC_ALL=zh_CN.UTF-8
export GTK_IM_MODULE=fcitx
export QT_IM_MODULE=fcitx
export XMODIFIERS=@im=fcitx
export DISPLAY=:0
export PULSE_SERVER=127.0.0.1
ENV_EOF
chmod +x /etc/profile.d/xxy_env.sh

# [5] 软件修复补丁
echo ">>> [内部] 应用 Firefox/VSCode 防闪退补丁..."
echo 'alias firefox="firefox-esr --no-remote"' >> /etc/bash.bashrc
# 创建桌面快捷方式修复
mkdir -p /usr/share/applications
sed -i 's/Exec=firefox-esr %u/Exec=firefox-esr --no-remote %u/g' /usr/share/applications/firefox-esr.desktop 2>/dev/null || true

# [6] 清理
apt autoremove -y; apt clean
echo ">>> [内部] 环境构建完成！"
EOF
    chmod +x "$TMP_WORKER"
}

# ==============================================================================
# 🔧 功能函数
# ==============================================================================

log() { echo -e "${G}[XXY] $1${NC}"; }
warn() { echo -e "${Y}[提示] $1${NC}"; }
err() { echo -e "${R}[错误] $1${NC}"; }

# --- 自动检测并修复 Shizuku 环境 ---
auto_fix_shizuku() {
    log "正在检查 Shizuku 环境..."
    
    # 1. 检查文件是否存在，不存在则下载 (使用国内源)
    if [ ! -f "$PREFIX/bin/rish" ] || [ ! -f "$PREFIX/bin/rish_shizuku.dex" ]; then
        echo -e "${Y}>>> 正在下载 rish 组件 (国内高速镜像)...${NC}"
        curl -sL "https://mirror.ghproxy.com/https://github.com/RikkaApps/Shizuku/releases/latest/download/rish" -o $PREFIX/bin/rish
        curl -sL "https://mirror.ghproxy.com/https://github.com/RikkaApps/Shizuku/releases/latest/download/rish_shizuku.dex" -o $PREFIX/bin/rish_shizuku.dex
        chmod +x $PREFIX/bin/rish
    fi

    # 2. 尝试执行
    log "正在尝试通过 Shizuku 解除进程限制..."
    echo -e "${Y}⚠️  注意：如果 Shizuku 弹出授权窗口，请务必点击【始终允许】！${NC}"
    
    if $PREFIX/bin/rish -c "/system/bin/device_config put activity_manager max_phantom_processes 2147483647 && cmd appops set com.termux RUN_IN_BACKGROUND allow" 2>/dev/null; then
        echo -e "${G}✅ 修复成功！Termux 已获得免死金牌。${NC}"
        read -n 1 -s -r -p "按任意键继续..."
    else
        echo -e "${R}❌ 授权失败。${NC}"
        echo "可能原因：Shizuku 未启动，或未授权。"
        echo "请确保 Shizuku APP 正在运行，然后再试一次。"
        read -n 1 -s -r -p "按任意键返回..."
    fi
}

# --- 安装系统 ---
install_system() {
    clear
    log "=== 安装 Debian 生产力系统 ==="
    # 检查依赖
    pkg install proot-distro pulseaudio termux-x11-nightly virglrenderer-android android-tools -y -o Dpkg::Options::="--force-confnew"

    if [ -d "$ROOTFS" ]; then
        read -p "系统已存在，是否删除重装? (y/n): " opt
        if [ "$opt" != "y" ]; then return; fi
        proot-distro remove "$DISTRO_NAME"
    fi

    log "正在下载系统 (请保持网络畅通)..."
    rm -rf "$PREFIX/var/lib/proot-distro/dlcache/$DISTRO_NAME"
    
    if proot-distro install "$DISTRO_NAME"; then
        echo "nameserver 223.5.5.5" > "$ROOTFS/etc/resolv.conf"
        create_worker_script
        log "正在进入容器配置环境 (Firefox/中文/桌面)..."
        proot-distro login "$DISTRO_NAME" --shared-tmp -- /bin/bash "$TMP_WORKER"
        rm -f "$TMP_WORKER"
        log "🎉 系统安装完成！"
    else
        err "下载失败。请开启 VPN 或检查网络。"
    fi
    read -n 1 -s -r -p "按任意键返回..."
}

# --- 安装 VSCode ---
install_vscode() {
    if [ ! -d "$ROOTFS" ]; then err "请先安装系统！"; sleep 1; return; fi
    log "正在下载并安装 VSCode (ARM64)..."
    
    # 注入安装脚本到容器
    cat > "$PREFIX/tmp/vscode_inst.sh" << 'EOF'
#!/bin/bash
cd /tmp
echo ">>> [内部] 下载 VSCode.deb..."
wget -O code.deb "https://update.code.visualstudio.com/latest/linux-deb-arm64/stable"
echo ">>> [内部] 安装中..."
apt install -y ./code.deb
rm code.deb
# 修复启动
echo ">>> [内部] 修复启动参数..."
echo 'alias code="code --no-sandbox --unity-launch"' >> /etc/bash.bashrc
sed -i 's/Exec=\/usr\/share\/code\/code/Exec=\/usr\/share\/code\/code --no-sandbox/g' /usr/share/applications/code.desktop 2>/dev/null || true
echo "✅ VSCode 安装完成。"
EOF
    proot-distro login "$DISTRO_NAME" --shared-tmp -- /bin/bash "$PREFIX/tmp/vscode_inst.sh"
    read -n 1 -s -r -p "按任意键返回..."
}

# --- 启动桌面 ---
start_desktop() {
    if [ ! -d "$ROOTFS" ]; then err "请先安装系统！"; sleep 1; return; fi
    clear
    log "🚀 正在启动生产力桌面..."
    
    # 清理 & 启动服务
    killall -9 termux-x11 Xwayland pulseaudio virgl_test_server_android >/dev/null 2>&1
    am start --user 0 -n com.termux.x11/com.termux.x11.MainActivity >/dev/null 2>&1
    
    termux-x11 :0 -ac &
    
    # 等待 X11 准备就绪
    local count=0
    while [ ! -S "$PREFIX/tmp/.X11-unix/X0" ]; do
        sleep 0.2
        count=$((count+1))
        if [ $count -gt 30 ]; then err "Termux:X11 启动失败，请检查是否安装了 APP"; return; fi
    done
    
    virgl_test_server_android &
    pulseaudio --start --load="module-native-protocol-tcp auth-ip-acl=127.0.0.1 auth-anonymous=1" --exit-idle-time=-1
    
    log "正在登入系统..."
    proot-distro login "$DISTRO_NAME" --shared-tmp -- bash -c "
        export DISPLAY=:0
        export PULSE_SERVER=127.0.0.1
        fcitx5 -d &
        dbus-launch --exit-with-session startxfce4
    "
}

# ==============================================================================
# 📺 菜单系统 (二级菜单设计)
# ==============================================================================

# 二级菜单：安装管理
menu_install() {
    while true; do
        clear
        echo -e "${B}┌── [安装管理] ──────────────┐${NC}"
        echo -e "1. ${G}一键安装 Debian 生产力版${NC} (含中文/桌面/浏览器)"
        echo -e "2. ${C}安装 VSCode${NC} (需先装系统)"
        echo -e "3. 卸载系统"
        echo -e "0. 返回上一级"
        echo -e "${B}└────────────────────────────┘${NC}"
        read -p "👉 选择: " sub_opt
        case "$sub_opt" in
            1) install_system ;;
            2) install_vscode ;;
            3) 
               read -p "确定删除吗? (y/n): " del_conf
               if [ "$del_conf" == "y" ]; then proot-distro remove "$DISTRO_NAME"; fi ;;
            0) return ;;
        esac
    done
}

# 二级菜单：维护工具
menu_maintenance() {
    while true; do
        clear
        echo -e "${B}┌── [维护工具] ──────────────┐${NC}"
        echo -e "1. ${Y}Shizuku 防闪退修复${NC} (全自动)"
        echo -e "2. 清理下载缓存 (修复安装失败)"
        echo -e "3. 备份系统 (打包为 tar.gz)"
        echo -e "0. 返回上一级"
        echo -e "${B}└────────────────────────────┘${NC}"
        read -p "👉 选择: " sub_opt
        case "$sub_opt" in
            1) auto_fix_shizuku ;;
            2) rm -rf "$PREFIX/var/lib/proot-distro/dlcache"; log "已清理。"; sleep 1 ;;
            3) log "暂未开放"; sleep 1 ;;
            0) return ;;
        esac
    done
}

# 主菜单
main_menu() {
    # 首次启动自检
    if [ ! -f "$PREFIX/etc/apt/sources.list.d/x11.list" ]; then pkg install x11-repo -y; fi
    
    while true; do
        clear
        echo -e "${B}┌────────────────────────────────────────┐${NC}"
        echo -e "${B}│         XXY v4.0 终极生产力工具箱      │${NC}"
        echo -e "${B}└────────────────────────────────────────┘${NC}"
        echo -e "当前状态: $(if [ -d "$ROOTFS" ]; then echo "${G}Debian 已就绪${NC}"; else echo "${R}未安装系统${NC}"; fi)"
        echo ""
        echo -e "1. ${G}🚀 启动桌面环境${NC} (日常使用点这个)"
        echo -e "2. 💿 安装/管理系统 (二级菜单)"
        echo -e "3. 🛠️ 维护/防闪退修复 (二级菜单)"
        echo -e "0. ❌ 退出"
        echo ""
        read -p "👉 请输入序号: " main_opt
        
        case "$main_opt" in
            1) start_desktop ;;
            2) menu_install ;;
            3) menu_maintenance ;;
            0) exit 0 ;;
            *) ;;
        esac
    done
}

# --- 启动 ---
main_menu

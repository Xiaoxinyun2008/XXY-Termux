# 1. 环境清理
rm -f $PREFIX/bin/xxy
rm -f $PREFIX/tmp/xxy_*
rm -rf $PREFIX/tmp/.X11-unix

# 2. 写入 v36.0 脚本
cat > $PREFIX/bin/xxy << 'INSTALL_EOF'
#!/data/data/com.termux/files/usr/bin/bash
# ==============================================================================
# PROJECT: XXY (Ultimate Edition v36.0)
# FIX: UI 遮挡修复、新增 Lynx 终端浏览器支持
# ==============================================================================

# --- 0. 全局配置 ---
export VERSION="v36.0"
export UPDATE_URL="https://raw.githubusercontent.com/Xiaoxinyun2008/XXY-Termux/main/xxy"
export GITHUB_URL="https://github.com/Xiaoxinyun2008/XXY-Termux"
export PREFIX="/data/data/com.termux/files/usr"
export TMPDIR="$PREFIX/tmp"
export ROOTFS_BASE="$PREFIX/var/lib/proot-distro/installed-rootfs"
export PD_CONF="$PREFIX/etc/proot-distro"
export TERMUX_PKG_OPTS="-y -o Dpkg::Options::=--force-confnew" 

[ ! -d "$TMPDIR" ] && mkdir -p "$TMPDIR"

R=$(tput setaf 1); G=$(tput setaf 2); Y=$(tput setaf 3)
B=$(tput setaf 4); P=$(tput setaf 5); C=$(tput setaf 6)
W=$(tput setaf 7); GR=$(tput setaf 8); NC=$(tput sgr0)

cleanup() {
    tput cnorm; stty echo; rm -f "$TMPDIR"/xxy_* 2>/dev/null
}
trap cleanup EXIT INT TERM

# --- 1. 基础函数 ---

get_ip() {
    local ip=""
    if command -v ip >/dev/null; then ip=$(ip route get 1.1.1.1 2>/dev/null | awk '{print $7}'); fi
    if [ -z "$ip" ] && command -v ifconfig >/dev/null; then
        ip=$(ifconfig 2>/dev/null | grep -oE 'inet (addr:)?([0-9]*\.){3}[0-9]*' | grep -v '127.0.0.1' | awk '{print $2}' | sed 's/addr://' | head -n 1)
    fi
    echo "${ip:-127.0.0.1}"
}

check_dependencies() {
    if [ -f "$PREFIX/etc/xxy_checked" ]; then return; fi
    clear; echo -e "${C}=== XXY 环境初始化 ===${NC}"
    # 新增 lynx 依赖检测，但为了启动速度，放到功能触发时再安装
    DEPS="proot-distro pulseaudio wget curl ncurses-utils grep sed awk fastfetch"
    
    if ! command -v fastfetch &>/dev/null; then pkg install fastfetch $TERMUX_PKG_OPTS >/dev/null 2>&1; fi
    
    MISSING=""
    for dep in $DEPS; do if ! command -v $dep &>/dev/null; then MISSING="$MISSING $dep"; fi; done
    if ! command -v termux-x11 &>/dev/null; then pkg install x11-repo $TERMUX_PKG_OPTS >/dev/null 2>&1; MISSING="$MISSING termux-x11-nightly"; fi
    
    if [ -n "$MISSING" ]; then
        echo -e "${P}🛠️  正在补全组件...${NC}"
        pkg update -y >/dev/null 2>&1 && pkg install $MISSING $TERMUX_PKG_OPTS
    fi
    touch "$PREFIX/etc/xxy_checked"
}

# 注入 GitHub 加速代理
optimize_network() {
    local PROXY_URL="https://mirror.ghproxy.com/"
    local TARGETS="ubuntu debian archlinux alpine"
    for distro in $TARGETS; do
        local conf_file="$PD_CONF/$distro.sh"
        if [ -f "$conf_file" ] && ! grep -q "mirror.ghproxy.com" "$conf_file"; then
            sed -i "s|TARBALL_URL=['\"]https://github.com|TARBALL_URL=\"${PROXY_URL}https://github.com|g" "$conf_file"
        fi
    done
}

# --- 2. 容器检测 ---

get_systems() {
    local raw_list=$(proot-distro list 2>/dev/null | sed 's/\x1b\[[0-9;]*m//g')
    local candidates=$(echo "$raw_list" | awk '/^Installed distributions:/{flag=1; next} /^Supported distributions:/{flag=0} flag && /\*/ {print $2}')
    for sys in $candidates; do
        if [ -f "$ROOTFS_BASE/$sys/bin/sh" ]; then echo "$sys"; fi
    done
}

# --- 3. 容器脚本 ---

WORKER_SCRIPT="$TMPDIR/xxy_worker_$$.sh"
INJECT_SCRIPT="$TMPDIR/xxy_inject_$$.sh"

create_worker_script() {
    rm -f "$WORKER_SCRIPT"
    cat << 'EOF' > "$WORKER_SCRIPT"
#!/bin/sh
export PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
action=$1
is_debian=false; is_arch=false; is_alpine=false
if command -v apt >/dev/null; then INSTALL="apt install -y"; UPDATE="apt update -y"; is_debian=true
elif command -v pacman >/dev/null; then INSTALL="pacman -S --noconfirm"; UPDATE="pacman -Sy --noconfirm"; is_arch=true
elif command -v apk >/dev/null; then INSTALL="apk add"; UPDATE="apk update"; is_alpine=true
else echo "Unknown OS"; exit 1; fi

case "$action" in
  change_source)
    if [ "$is_debian" = true ]; then
        sed -i 's/http:\/\/.*archive.ubuntu.com/http:\/\/mirrors.tuna.tsinghua.edu.cn/g' /etc/apt/sources.list 2>/dev/null
        sed -i 's/http:\/\/ports.ubuntu.com/http:\/\/mirrors.tuna.tsinghua.edu.cn/g' /etc/apt/sources.list 2>/dev/null
        sed -i 's/http:\/\/deb.debian.org/http:\/\/mirrors.ustc.edu.cn/g' /etc/apt/sources.list 2>/dev/null
    elif [ "$is_alpine" = true ]; then
        sed -i 's/dl-cdn.alpinelinux.org/mirrors.ustc.edu.cn/g' /etc/apk/repositories
    fi
    $UPDATE ;;
  install_gui)
    if [ "$is_debian" = true ]; then DEBIAN_FRONTEND=noninteractive $INSTALL xfce4 xfce4-goodies dbus-x11 tigervnc-standalone-server
    elif [ "$is_arch" = true ]; then $INSTALL xfce4 dbus tigervnc
    elif [ "$is_alpine" = true ]; then $INSTALL xfce4 dbus-x11 tigervnc; fi
    mkdir -p ~/.vnc; echo "123456" | vncpasswd -f > ~/.vnc/passwd; chmod 600 ~/.vnc/passwd ;;
  install_fastfetch)
    $INSTALL fastfetch || $INSTALL neofetch
    if command -v fastfetch >/dev/null; then echo "fastfetch" > /tmp/fetch_bin
    elif command -v neofetch >/dev/null; then echo "neofetch" > /tmp/fetch_bin
    else echo "none" > /tmp/fetch_bin; fi ;;
  install_cn)
    if [ "$is_debian" = true ]; then DEBIAN_FRONTEND=noninteractive $INSTALL locales fonts-noto-cjk ttf-wqy-zenhei; echo "zh_CN.UTF-8 UTF-8" > /etc/locale.gen && locale-gen; echo "export LANG=zh_CN.UTF-8" > /etc/profile.d/xxy_lang.sh; fi ;;
esac
EOF
    chmod 755 "$WORKER_SCRIPT"
}

run_task() {
    local sys=$1; local cmd=$2; local desc=$3
    local inner_script="/tmp/$(basename "$WORKER_SCRIPT")"
    tput cup 23 0; echo -ne "${Y}⚙️  [$sys] $desc...${NC}"
    if proot-distro login "$sys" --shared-tmp -- /bin/sh "$inner_script" "$cmd" >/dev/null 2>&1; then
        tput cup 23 0; echo -ne "${G}✅ [$sys] $desc 完成          ${NC}"
    else
        tput cup 23 0; echo -ne "${R}❌ [$sys] $desc 失败          ${NC}"
    fi
}

select_menu() {
    mapfile -t SYS_ARRAY < <(get_systems)
    if [ ${#SYS_ARRAY[@]} -eq 0 ]; then
        echo -e "\n${R}⚠️  未检测到系统！${NC} (请先 [2] 安装)"
        read -n 1 -s -r -p "..."
        return 1
    fi
    for i in "${!SYS_ARRAY[@]}"; do echo -e "${G}$((i+1)).${NC} ${SYS_ARRAY[$i]}"; done
    echo -e "${W}0.${NC} 返回"
    tput cnorm; stty echo; read -p "👉 序号: " choice; stty -echo; tput civis
    if [ "$choice" == "0" ] || [ -z "$choice" ]; then return 1; fi
    if [[ "$choice" =~ ^[0-9]+$ ]] && [ "$choice" -ge 1 ] && [ "$choice" -le "${#SYS_ARRAY[@]}" ]; then
        echo "${SYS_ARRAY[$((choice-1))]}"
        return 0
    fi
    return 1
}

# --- 功能模块 ---

do_login() {
    clear; echo -e "${C}=== 登录系统 ===${NC}"
    TARGET=$(select_menu) || return
    create_worker_script
    local inner_script="/tmp/$(basename "$WORKER_SCRIPT")"
    local inner_inject="/tmp/$(basename "$INJECT_SCRIPT")"
    echo -e "${B}配置环境中...${NC}"
    proot-distro login "$TARGET" --shared-tmp -- /bin/sh "$inner_script" "install_fastfetch" >/dev/null 2>&1
    cat > "$INJECT_SCRIPT" << 'EOF'
RC=~/.bashrc
[ -f /etc/profile.d/xxy_lang.sh ] && . /etc/profile.d/xxy_lang.sh
if [ -f /tmp/fetch_bin ]; then
    TOOL=$(cat /tmp/fetch_bin); rm -f /tmp/fetch_bin
    if [ "$TOOL" != "none" ] && ! grep -q "$TOOL" "$RC" 2>/dev/null; then
        echo "" >> "$RC"; echo "clear" >> "$RC"; echo "$TOOL" >> "$RC"
    fi
fi
EOF
    proot-distro login "$TARGET" --shared-tmp -- /bin/sh "$inner_inject" >/dev/null 2>&1
    cleanup; proot-distro login "$TARGET"; tput civis; stty -echo; draw_ui
}

do_install() {
    clear; echo -e "${C}=== 安装系统 ===${NC}"
    DISTROS=("ubuntu" "debian" "archlinux" "alpine")
    for i in "${!DISTROS[@]}"; do echo -e "${W}$((i+1)).${NC} ${DISTROS[$i]}"; done
    echo -e "${W}0.${NC} 返回"
    tput cnorm; stty echo; read -p "👉 选择: " idx; stty -echo; tput civis
    if [ "$idx" == "0" ]; then return; fi
    if [[ "$idx" =~ ^[0-9]+$ ]] && [ "$idx" -ge 1 ] && [ "$idx" -le "${#DISTROS[@]}" ]; then
        TARGET=${DISTROS[$((idx-1))]}
        if [ -d "$ROOTFS_BASE/$TARGET" ]; then
            if [ ! -f "$ROOTFS_BASE/$TARGET/bin/sh" ]; then
                echo -e "\n${Y}清理残留...${NC}"; proot-distro remove $TARGET >/dev/null 2>&1; rm -rf "$ROOTFS_BASE/$TARGET"
            else echo -e "\n${G}已存在！${NC}"; read -n 1 -s -r -p "..."; return; fi
        fi
        
        optimize_network # 注入加速
        echo -e "\n${Y}下载中 (加速)...${NC}"; tput cnorm
        if proot-distro install $TARGET; then
            tput civis; create_worker_script
            run_task "$TARGET" "change_source" "换国内源"
            run_task "$TARGET" "install_cn" "中文环境"
            run_task "$TARGET" "install_fastfetch" "美化工具"
            echo -e "\n${G}🎉 安装完成！${NC}"; read -n 1 -s -r -p "..."
        else
            echo -e "\n${R}❌ 失败，正在清理...${NC}"
            proot-distro remove $TARGET >/dev/null 2>&1; rm -rf "$ROOTFS_BASE/$TARGET"
            read -n 1 -s -r -p "..."
        fi
    fi
}

do_gui() {
    clear; echo -e "${C}=== 启动桌面 ===${NC}"
    TARGET=$(select_menu) || return
    create_worker_script
    echo -ne "🔍 检查组件...\r"
    if ! proot-distro login "$TARGET" -- bash -c "command -v startxfce4" >/dev/null 2>&1; then
        run_task "$TARGET" "install_gui" "安装 XFCE+VNC"
    fi
    echo -e "\n${G}模式:${NC}\n1. Termux:X11\n2. VNC Viewer\n0. 返回"
    tput cnorm; stty echo; read -p "选择: " m; stty -echo; tput civis
    if [ "$m" == "1" ]; then
        pulseaudio --kill >/dev/null 2>&1; pkill -f com.termux.x11 >/dev/null 2>&1; rm -rf "$TMPDIR/.X11-unix"
        pulseaudio --start --load="module-native-protocol-tcp auth-ip-acl=127.0.0.1 auth-anonymous=1" --exit-idle-time=-1
        am start --user 0 -n com.termux.x11/com.termux.x11.MainActivity >/dev/null 2>&1; sleep 1
        termux-x11 :0 >/dev/null 2>&1 &
        proot-distro login "$TARGET" --shared-tmp -- bash -c "export DISPLAY=:0 PULSE_SERVER=127.0.0.1; dbus-launch --exit-with-session startxfce4"
    elif [ "$m" == "2" ]; then
        CMD="vncserver -kill :1 >/dev/null 2>&1; rm -rf /tmp/.X1-lock /tmp/.X11-unix/X1; vncserver :1 -geometry 1280x720"
        proot-distro login "$TARGET" -- bash -c "$CMD"
        echo -e "\n${G}VNC ON${NC}: ${W}127.0.0.1:5901${NC} | ${W}$(get_ip):5901${NC}\nCode: 123456"; read -n 1 -s -r -p "..."
    fi
}

# [F6] 终端浏览器访问 GitHub (新功能)
do_github() {
    clear; echo -e "${C}=== 访问 GitHub ===${NC}"
    echo -e "正在启动终端浏览器..."
    
    # 自动配置相关库
    if ! command -v lynx &>/dev/null; then
        echo -e "${Y}检测到未安装浏览器，正在自动配置 lynx ...${NC}"
        tput cnorm
        pkg install lynx $TERMUX_PKG_OPTS
        tput civis
    fi
    
    echo -e "${G}正在跳转至项目主页... (按 Q 退出)${NC}"
    sleep 1
    # 启动 lynx 访问
    lynx "$GITHUB_URL"
    
    # 退出后重绘 UI
    tput civis; stty -echo; draw_ui
}

do_update() {
    clear; echo -e "${C}=== 检查更新 ===${NC}"
    local temp="$TMPDIR/xxy_tmp"; rm -f "$temp"
    if [ "$(curl -s -o "$temp" -w "%{http_code}" "$UPDATE_URL")" != "200" ]; then
        echo -e "\n${R}❌ 网络错误${NC}"; read -n 1 -s -r -p "..."; return
    fi
    if ! grep -q "PROJECT: XXY" "$temp"; then echo -e "\n${R}❌ 校验失败${NC}"; read -n 1 -s -r -p "..."; return; fi
    REMOTE=$(grep "export VERSION=" "$temp" | cut -d'"' -f2)
    if [[ "$REMOTE" != "$VERSION" ]]; then
        echo -e "\n${Y}新版本: $REMOTE${NC}"; tput cnorm; stty echo; read -p "更新? (y/n): " c; stty -echo; tput civis
        if [[ "$c" == "y" ]]; then mv "$temp" "$PREFIX/bin/xxy"; chmod 755 "$PREFIX/bin/xxy"; echo -e "${G}完成，请重启。${NC}"; exit 0; fi
    else echo -e "\n${G}已是最新 ($VERSION)${NC}"; fi
    read -n 1 -s -r -p "..."
}

# --- Main UI ---
update_status() {
    if (( FRAME % 50 == 0 )); then MEM=$(free -m 2>/dev/null | awk 'NR==2{printf "%.1fG", $3/1024}' || echo "N/A"); fi
    tput cup 0 34; printf "${B}╭── STATUS ──────────────────────────╮${NC}"
    tput cup 1 34; printf "${B}│${NC} TIME: ${W}%-5s${NC} MEM: ${P}%-5s${NC}         ${B}│${NC}" "$(date "+%H:%M")" "$MEM"
    tput cup 2 34; printf "${B}│${NC} IP: ${GR}%-15s${NC}             ${B}│${NC}" "$(get_ip)"
    tput cup 3 34; printf "${B}╰────────────────────────────────────╯${NC}"
}

draw_ui() {
    clear
    cat << EOF
${C}
██╗  ██╗██╗  ██╗██╗   ██╗ ${W}XXY Termux${C}
╚██╗██╔╝╚██╗██╔╝╚██╗ ██╔╝ ${GR}${VERSION}${C}
 ╚███╔╝  ╚███╔╝  ╚████╔╝  ${W}Ultimate${C}
 ██╔██╗  ██╔██╗   ╚██╔╝   ${NC}
${GR}──────────────────────────────────────────${NC}
${GR}│${NC} ${W}1.${NC} 登录系统 (Login)    ${GR}│${NC} ${W}3.${NC} 启动桌面 (GUI)
${GR}│${NC} ${W}2.${NC} 安装系统 (Install)  ${GR}│${NC} ${W}5.${NC} 检查更新 (Update)
${GR}│${NC} ${W}6.${NC} 访问 GitHub (Lynx)  ${GR}│${NC} ${W}0.${NC} 退出程序 (Exit)
${GR}──────────────────────────────────────────${NC}
EOF
    # 限制 Fastfetch 行数防止遮挡，只显示 OS, Kernel, Uptime
    if command -v fastfetch >/dev/null; then
        fastfetch --config structure:OS:Kernel:Uptime --logo none 2>/dev/null | sed 's/^/  /'
    fi
}

check_dependencies; create_worker_script; tput civis; stty -echo
draw_ui; INPUT_BUF=""; FRAME=0; MEM="Wait"
while true; do
    update_status; ((FRAME++))
    # [UI修复] 将输入框移到第 22 行，防止遮挡菜单
    tput cup 22 0; echo -ne "${C}➤ 序号: ${W}${INPUT_BUF}_        ${NC}"
    read -t 0.05 -n 1 key
    if [[ $? -eq 0 ]]; then
        if [[ -z "$key" ]]; then
            if [[ -n "$INPUT_BUF" ]]; then
                case $INPUT_BUF in
                    1)do_login;; 2)do_install;; 3)do_gui;; 5)do_update;; 6)do_github;; 0)exit 0;;
                esac
                tput civis; stty -echo; draw_ui; INPUT_BUF=""
            fi
        elif [[ "$key" == $'\x7f' || "$key" == $'\b' ]]; then INPUT_BUF="${INPUT_BUF%?}"
        elif [[ "$key" == $'\e' ]]; then read -t 0.01 -n 2
        else INPUT_BUF="$INPUT_BUF$key"; fi
    fi
done
INSTALL_EOF

chmod 755 $PREFIX/bin/xxy
sed -i 's/\r$//' $PREFIX/bin/xxy
echo -e "\033[1;32m✅ XXY v36.0 更新完成！\n修复了菜单显示不全的问题，并增加了 GitHub 浏览器。\n输入 xxy 启动。\033[0m"
exec xxy
